import React, { useEffect } from "react";
import styled from "styled-components";

const GameRoot = styled.div``;

const Game = () => {
  return (
    <GameRoot>
      <div>sdfsd</div>
    </GameRoot>
  );
};

export default Game;
